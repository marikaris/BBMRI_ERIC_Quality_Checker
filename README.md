# BBMRI_ERIC_Quality_Checker
To run: include a config.txt in the directory of this project (put it in the .gitignore when pushing).
The file should look like: config_example.txt
Now run qualityChecker.py